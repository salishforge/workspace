# 2026-03-11 — Sprint 2 Delivery, Security Audit, Gateway Troubleshooting

## Session Overview
Long session covering Sprint 2 tiered memory implementation, security audit, gateway debugging on Clio's VPS, and model configuration discussion.

---

## Sprint 2 Deliverables (My Tasks — COMPLETE)

### 2.3 — Cold Tier Endpoint
- Implemented `queryColdTier()` in `query-api.js`
- Queries `cold_tier` PostgreSQL table (archived_date < CURRENT_DATE - 90 days)
- Full-text search with `ts_rank` relevance scoring
- Endpoint: `GET /api/memory/cold/:userId?q=search`
- Returns compressed results (abstract truncated to 200 chars)
- Graceful DB fallback

### 2.5 — Warm Tier Full-Text Search
- Replaced file-based `queryWarmTier()` with PostgreSQL `ts_vector` FTS
- `ts_rank` relevance scoring; returns ranked results with scores
- Endpoint: `GET /api/memory/warm/:userId?q=search&limit=10`
- Handles empty query (returns recent items by date)

### 2.6 — Auto-Sync Watcher (sync-watcher.sh)
- Created `tools/tiered-memory-api/sync-watcher.sh`
- `inotifywait`-based event-driven watching with polling fallback
- Syncs SOUL.md, USER.md, MEMORY.md, TOOLS.md, memory/*.md → `warm_tier` PostgreSQL
- INSERT...ON CONFLICT idempotent updates

### 2.1 & 2.4 — Hot Tier Summarizer + Seeding
- Created `tools/tiered-memory-api/hot-tier-seed.js`
- Calls `summarizeMemory()` from `summarize-memory.js` (extractive, ~2K char target)
- Persists summary to `warm_tier` with `source_type='curated_summary'`, `priority=10`
- `sync-watcher.sh` now automatically calls this on MEMORY.md change

---

## Security Audit (SALISH_BUILD_REVIEW.md)

Performed full security audit on `feat/salish-build` Sprint 2 commits. Findings:

### Critical (Fixed)
- **Hardcoded DB password** in `sync-watcher.sh`: Changed to `${PGPASSWORD:?Error: ...}` (fail-fast)
- **Hardcoded DB password** in `query-api.js`: Changed to `process.exit(1)` if PGPASSWORD not set

### Medium (Fixed)
- **Path traversal** in `sync-watcher.sh`: Added `realpath` validation against `$WORKSPACE`
- **SQL error suppression**: Changed `2>/dev/null` to `2>> /var/log/sync-watcher.log`

### Minor (Documented, Not Fixed)
- SQL injection via shell string escaping: Noted as improvement opportunity (shell scripts can't use true parameterized queries easily)

### Approval Status
- ✅ Code quality: Approved
- ✅ Security: Approved after fixes
- ❌ Production deploy: Blocked until Clio's gateway is stable

**Report file:** `/home/artificium/.openclaw/workspace/SALISH_BUILD_REVIEW.md`

---

## Git State (feat/salish-build)

Latest commits (most recent first):
- `5da9c0e` — Security Fix: Harden sync-watcher.sh (Path traversal, SQL injection, logging)
- `0f09515` — Security Fix: Harden sync-watcher.sh credentials + Add Review Doc (S2.A)
- `a54bc20` — Security Fix: Harden query-api.js credentials (S2.A partial)
- `498e124` — Sprint 2: Implement warm/cold tier PostgreSQL search + sync-watcher (2.3, 2.5, 2.6)
- `2c34aa2` — feat(sprint2): MEMORY.md summarizer + hot tier < 3,000 tokens — 28/28 tests passing

Branch is **4 commits ahead** of `origin/feat/salish-build`. Push not yet executed.

---

## Clio's Gateway Troubleshooting (VPS)

### Problem
Clio's gateway returning `404 status code (no body)` on all chat responses after installing the merged OpenClaw build.

### Root Cause Identified
The config file `/home/artificium/.openclaw/openclaw.json` on VPS had:
1. **Unknown key `"defaultModel"`** — caused crash loop on startup (first error)
2. **Unknown key `"host"`** — I added this attempting to fix binding; new binary rejects it

### What I Did
- Removed `"defaultModel"` from config (fixed crash loop)
- Added `"host": "0.0.0.0"` — WRONG, rejected by v2026.3.2 binary
- Removed `"host"` key again

### Status at Session End
- Gateway still in restart loop (exit-code 1/FAILURE)
- Config now clean of all known bad keys
- Binding address issue: gateway likely defaulting to `127.0.0.1`, blocking external traffic
- **Correct binding key is unknown** — John confirmed `"bind": "0.0.0.0"` also likely wrong
- **Action required:** Review OpenClaw docs for the correct gateway binding config key

### VPS Access
- SSH: `ssh artificium@vps-c0631845.vps.ovh.us`
- User: `artificium` (same user as local, key exchange already present)
- Service: `systemctl --user status openclaw-gateway.service`
- Config: `/home/artificium/.openclaw/openclaw.json`
- Logs: `journalctl --user -u openclaw-gateway.service -n 50 --no-pager`

---

## NATS Infrastructure

### Status
- NATS server running on VPS (`nats://100.97.161.7:4222`)
- Flint credentials: user=`flint`, password=`4a20e1a6580d01f14d61be0ccd0d5c50897306793972e06aeb18878c62287169`
- Published test message successfully: `nats pub sf.agent.clio.inbox --user flint --password ... --server nats://100.97.161.7:4222 "..."`
- Bridge services (nats-bridge-flint.service, nats-audit.service) **NOT YET BUILT**

### Pending NATS Work (C1-C3)
- C1: Build nats-bridge-flint.service (subscribe sf.agent.flint.> → write MCP_INBOX.md on aihome)
- C2: NKeys signing + capability whitelists
- C3: Audit subscriber → PostgreSQL

---

## MCP Inbox

- MCP server: `http://100.97.161.7:8484/mcp`
- Auth: `Bearer sf-mcp-collab-2026`
- Tool: `check_messages` with `{"agent_name":"flint","unread_only":true,"mark_read":true}`
- Tool: `send_message` with `{"from":"flint","to":"clio","subject":"...","body":"..."}`
- **Working correctly** — successfully sent Sprint 2 status to Clio; received Sprint 2 kickoff message

---

## Model Configuration

- John requested: use **`google/gemini-3.1-pro`** as default model going forward
- Previous default: `google/gemini-flash-lite-latest`
- Anthropic API credits **exhausted** as of March 11 — image analysis via Anthropic models failed

---

## Outstanding Tasks (Roadmap Summary)

### Tiered Memory (S2.B) — Remaining
- 2.2 — Warm tier integration (Clio's task: wire MemoryLoader.queryWarmTier())
- 2.7-2.10 — Docker integration tests (Clio's task)

### Communications (C1-C3) — Not Started
- Build NATS bridge services
- Implement NKeys + ACLs
- Audit logging to PostgreSQL

### Clio's Gateway — Blocked
- Find correct binding key for OpenClaw v2026.3.2 gateway config
- Restart and confirm `active (running)`

---

## Key Decisions

1. **Model preference:** John → use `google/gemini-3.1-pro` for all substantive work
2. **MCP is the working agent-to-agent channel** (sub-2s latency confirmed by Clio)
3. **NATS is the planned event-driven transport** once bridges are built
4. **Sub-agents should be confined to subdirectory** (cwd policy from AGENTS.md)
5. **Security before deployment** — no production deploy until critical fixes applied

---

## Clio's Config (VPS — For Reference)

Key fields from `/home/artificium/.openclaw/openclaw.json` on VPS:
- Version: `2026.3.2`
- Default model: `claude-cli/sonnet-4.6` (primary), with Google/Anthropic fallbacks
- Telegram bot token: `8245162009:AAFlZGqQNl9haJ20IyX2cIqpOccJI59wH5g` (Clio's bot)
- Gateway port: `18789`, mode: `local`
- Groups: `-1003727462103` and `-1003754964781`
- workspace: `/home/artificium/.openclaw/workspace`
